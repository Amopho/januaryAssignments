// Assignments starting from line....

const add = ["Berlin", "Paris", "Prague", "Malaga", "Hi"];

// Reference - a "copy" affects the original array

// .slice()
// to make a shallow copy
let shallowCopy = add.slice(1); //assigning
console.log(shallowCopy);
console.log(add);

// Assignments part

// Ex1 before Midday
// Repeat it. 
// Create a program with two variables: “item” and “times”. Create a program that repeats the “item” as many times as specified by “times”.
let item = [];
let
for (let i= 0, i< TimeRanges.length; i++)
// The first variable (“item”) is the item that needs repeating while the second argument (“times”) is the number of times the item is to be repeated. Print the result in an array. Examples:
// (“example”, 3) ➞ [“example”, “example”, “example”]

// Ex2 before Midday

//

const findGreatest = (arr, num) => {
    let result = [];
    for (let i=0; )
}